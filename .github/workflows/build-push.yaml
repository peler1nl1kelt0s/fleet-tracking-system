name: Build and Push Docker Images

on:
  push:
    branches: [ "dev" ]
    paths:
      - 'back-end/**'
      - 'front-end/fleet-tracking-system/**'
      - '.github/workflows/build-push.yaml'

env:
  REGISTRY: ghcr.io
  IMAGE_OWNER: ${{ github.repository_owner }}

jobs:
  build-backend:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check if backend changed
        id: backend-changed
        uses: dorny/paths-filter@v2
        with:
          filters: |
            backend:
              - 'back-end/**'

      - name: Log in to GitHub Container Registry
        if: steps.backend-changed.outputs.backend == 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Backend
        if: steps.backend-changed.outputs.backend == 'true'
        uses: docker/build-push-action@v5
        with:
          context: ./back-end
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/${{ github.event.repository.name }}-backend:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/${{ github.event.repository.name }}-backend:${{ github.sha }}
          no-cache: true

  build-frontend:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Check if frontend changed
        id: frontend-changed
        uses: dorny/paths-filter@v2
        with:
          filters: |
            frontend:
              - 'front-end/fleet-tracking-system/**'

      - name: Log in to GitHub Container Registry
        if: steps.frontend-changed.outputs.frontend == 'true'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Frontend
        if: steps.frontend-changed.outputs.frontend == 'true'
        uses: docker/build-push-action@v5
        with:
          context: ./front-end/fleet-tracking-system
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/${{ github.event.repository.name }}-frontend:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/${{ github.event.repository.name }}-frontend:${{ github.sha }}
          no-cache: true